{
  "name": "crime-team-orchestrator",
  "version": "0.1.0",
  "description": "Multi-agent orchestrator on top of OpenClaw — for the Crime OS · MERIDIAN//OS myproject project. Dispatches a task to a Producer agent, parses DISPATCH blocks, runs specialists in parallel, integrates replies.",
  "private": true,
  "type": "module",
  "bin": {
    "crime-team": "./bin/crime-team.mjs"
  },
  "scripts": {
    "build": "tsc",
    "start": "node ./dist/cli.js",
    "dev": "tsx src/cli.ts"
  },
  "dependencies": {
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.6.0"
  }
}
