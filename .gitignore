# Dependencies
node_modules/
desktop/node_modules/

# Build output
dist/
desktop/src-tauri/target/
desktop/dist/
*.tsbuildinfo

# Local state — never commit
runs/
.crime-team.local.json
*.log

# Secrets — should never exist anyway; belt + suspenders
.env
.env.*
!.env.example

# OpenClaw workspace bootstrap pollution — never commit these
# (OpenClaw drops these in any CWD it sees as an "agent workspace")
.openclaw/
BOOTSTRAP.md
AGENTS.md
HEARTBEAT.md
IDENTITY.md
SOUL.md
TOOLS.md
USER.md
workspace-state.json

# Editor / OS
.vscode/local/
.idea/
.DS_Store
Thumbs.db
*.bak
*.tmp
